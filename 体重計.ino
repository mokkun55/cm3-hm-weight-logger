// Arduino CLI requires a sketch file that matches the directory name.
// The actual firmware implementation lives in src/main.cpp so the same
// project can be used from both Arduino CLI and PlatformIO.
